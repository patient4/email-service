{
  "name": "everflow-backend",
  "version": "1.0.0",
  "description": "Serverless function for Everflow Logistics",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0"
  }
}
